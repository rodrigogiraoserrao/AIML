Going to try and use genetic algorithms to breed a robot that chooses where to go to clean a house.

Running the class BasicSimulator now runs (a version of) the genetic algorithm and produces increasingly good cleaning robots.
